var impares=[];
var pares=[];
function par_impar(numero_aleatorio) {
	if (numero_aleatorio%2==0) {
		pares.push(numero_aleatorio);
	} else { impares.push(numero_aleatorio);}
// body...	}
var numero_aleatorio= Math.floor((Math.random()*100)+1);
for(var i=0; i<50; i++){
	return numero_aleatorio;	
	par_impar(numero_aleatorio);
}